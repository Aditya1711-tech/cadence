# Keep the Gradle wrapper script LF so it runs in the Linux Docker build + mac/linux.
gradlew      text eol=lf
*.sh         text eol=lf
*.bat        text eol=crlf
*.jar        binary
# SQL migrations + yaml: normalize to LF for consistent cross-platform hashes.
*.sql        text eol=lf
*.yml        text eol=lf
