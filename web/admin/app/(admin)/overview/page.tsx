import { OverviewBody } from "@/components/overview/overview-body";

// P2-E.5 — team summary: category heatmap + AI token spend + commit activity.
export default function OverviewPage() {
  return <OverviewBody />;
}
